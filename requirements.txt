# FastAPI
fastapi==0.115.6
uvicorn[standard]==0.32.1

# OpenAI (image generation) + Anthropic (flowchart conversation/extraction)
openai>=1.0.0
anthropic>=0.40.0

# Supabase Auth (no Prisma)
supabase==2.10.0

# Image optimization (WebP conversion)
Pillow>=10.0.0
httpx>=0.27.0

# Env & validation
python-dotenv==1.0.1
pydantic[email-validator]==2.10.3
pydantic-settings==2.6.1
email-validator>=2.0.0
eval_type_backport>=0.2.0  # Python 3.9 compat for X | Y annotation syntax